input CreateUserInput {
  name: String!
  email: String!
  age: Int!
}

type User {
  id: ID!
  name: String!
  email: String!
  age: Int!
}

type Mutation {
  createUser(input: CreateUserInput!): User!
}
